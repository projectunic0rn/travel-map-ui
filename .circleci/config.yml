version: 2.1
orbs:
  eslint: arrai/eslint@1.3.0
jobs:
  build:
    docker:
      - image: circleci/node:7
    steps:
      - checkout
      - run: npm install
      - run: eslint .
      - run: npm test
